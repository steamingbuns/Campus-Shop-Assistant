fastapi
uvicorn[standard]
spacy
python-multipart
# Database + NLP helpers
psycopg2-binary
# Add any extra production packages here (gunicorn, prometheus client, etc.)
# Add any extra production packages here (gunicorn, prometheus client, etc.)
